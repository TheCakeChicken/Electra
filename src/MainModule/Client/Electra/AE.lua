client = nil
service = nil

return function()
    client.AE = {

        Detected = function (Action, Info)
           
        end;

        --// Client log detection (will use remotes for communication later)


    }
end